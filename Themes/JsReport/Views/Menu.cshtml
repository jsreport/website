@{
    // Model is Model.Menu from the layout (Layout.Menu)
    Model.Classes.Add("menu nav nav-pills pull-left");
    var tag = Tag(Model, "ul");
}


    @tag.StartElement
        @* see MenuItem shape template *@
        @DisplayChildren(Model)
    @tag.EndElement
